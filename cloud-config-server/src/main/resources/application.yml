server:
  port: 9296

spring:
  application:
    name: CONFIG-SERVER
  cloud:
    config:
      server:
        git:
          uri: https://github.com/mshall/microservice-skeleton/tree/main/cloud-config-server-repo
          clone-on-start: true
